@model IEnumerable<WebApplication1.Models.Besuch>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Beschreibung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kunde)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Beschreibung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Consultant.ConsultantId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kunde.KundeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BesuchId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BesuchId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BesuchId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
